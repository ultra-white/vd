{
  "collectionName": "components_order_products",
  "info": {
    "displayName": "Товар",
    "icon": "archive"
  },
  "options": {},
  "attributes": {
    "quantity": {
      "type": "integer",
      "required": true
    },
    "size": {
      "type": "enumeration",
      "required": true,
      "enum": [
        "XS",
        "S",
        "M",
        "L"
      ]
    },
    "product": {
      "type": "relation",
      "relation": "oneToOne",
      "target": "api::product.product"
    }
  },
  "config": {}
}
